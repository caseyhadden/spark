apply plugin: 'application'

dependencies {
    compile project(':spark-api')
    compile 'com.yammer.dropwizard:dropwizard-db:0.5.1'
    runtime 'com.h2database:h2:1.3.168'
}

mainClassName = 'com.caseyhadden.spark.SparkService'

run {
    args 'server', 'src/dist/spark.yml'
}